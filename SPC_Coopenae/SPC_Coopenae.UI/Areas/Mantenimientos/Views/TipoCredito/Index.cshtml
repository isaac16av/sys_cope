@model IEnumerable<SPC_Coopenae.UI.Areas.Mantenimientos.Models.TipoCredito>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Registrar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTipoCredito)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComisionDistinta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaximoComision)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaximoComision)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTipoCredito)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComisionDistinta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaximoComision)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SumaIDP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.IdTipoCredito }) |
            @Html.ActionLink("Detalles", "Detalles", new { id = item.IdTipoCredito }) |
            @Html.ActionLink("Eliminar", "Eliminar", new { id = item.IdTipoCredito })
        </td>
    </tr>
}

</table>
